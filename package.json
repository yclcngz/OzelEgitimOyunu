{
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx serve www -l 3000",
    "cap:sync": "node build.js && npx cap sync",
    "cap:android": "npx cap open android",
    "optimize": "node optimize_images.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "esbuild": "^0.28.0",
    "sharp": "^0.34.5"
  }
}
